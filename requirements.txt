# Phụ thuộc lõi — chạy được trên VPS không GPU (preprocess, graph, test).
# KHÔNG chứa torch: xem requirements-train.txt.
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
pyarrow==18.1.0
PyYAML==6.0.2
pydantic==2.10.3
pytest==8.3.4
