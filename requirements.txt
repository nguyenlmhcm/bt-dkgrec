# Phụ thuộc lõi — VPS: sửa code, preprocess, dựng graph, chạy test.
# KHÔNG chứa torch, và VPS KHÔNG cài torch (D28). Mọi thứ cần torch chạy
# trên Colab, nơi torch bản CUDA có sẵn — xem requirements-colab.txt và D25.
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
pyarrow==18.1.0
PyYAML==6.0.2
pydantic==2.10.3
matplotlib==3.9.3
pytest==8.3.4

# Sinh tai lieu Word (scripts 08 va 10). CHI can tren VPS — khong dua vao
# requirements-colab.txt vi Colab chi train, khong ghi .docx.
python-docx==1.2.0
