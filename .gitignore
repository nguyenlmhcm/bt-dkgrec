# ── Dữ liệu: KHÔNG BAO GIỜ commit (raw ~1.3 GB) ─────────────────────
# Neo o goc repo: mau 'data/' khong neo se nuot ca configs/data/ va src/data/.
/data/
/Datasets_RetailRocket_Raw/

# ── Run artifact: tách theo độ nặng ─────────────────────────────────
# Git   metrics.json, curves.csv, config.yaml, seed.txt
#       → nhẹ, cần version control, Bước 9 `make tables` đọc lại từ đây
# Drive topk.csv, train.log
#       → nặng; topk.csv chỉ demo cần đọc, train.log chỉ dùng khi chẩn đoán
experiments/runs/**/topk.csv
experiments/runs/**/train.log

# ── Hình: sinh lại được từ run artifact, không commit ───────────────
experiments/figures/

# ── Paper co ban quyen: giu local, KHONG commit ─────────────────────
*.pdf

# ── Python ─────────────────────────────────────────────────────────
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/

# ── Hệ điều hành / editor ──────────────────────────────────────────
.DS_Store
.vscode/
.idea/
